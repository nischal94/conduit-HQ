# Invariants ledger

The spec's load-bearing claims — security invariants and product promises —
are pinned by named tests, not by convention or reviewer vigilance. When one
of these tests fails, the failure output names the spec section being
violated.

**Naming:** invariant tests carry an `INVARIANT §x.y:` prefix in their
test name.

**Rule (see CLAUDE.md):** a module that implements a spec invariant may not
land without its invariant test in the same commit, and this ledger's row
flips in that same commit. Unpinned claims stay visibly ⏳ — a ⏳ row is a
promise the product makes but does not yet enforce.

| Spec claim | Pinning test | Status |
| --- | --- | --- |
| §4.2 / §8 — search hits never carry schemas | `packages/sdk/src/catalog.test.ts` | ✅ pinned |
| §8 — schemas load only on explicit describe (lazy) | `packages/sdk/src/catalog.test.ts` | ✅ pinned |
| §10.1 — riskClass default mapping table | `packages/sdk/src/risk.test.ts` | ✅ pinned |
| §7 — policies persist across source refresh; manual overrides never silently reverted | `packages/sdk/src/store/sqlite.test.ts` | ✅ pinned |
| §9.2 — secrets encrypted at rest (master key) | `packages/sdk/src/store/sqlite.test.ts` + `secrets.test.ts` | ✅ pinned |
| §4.2 — execute surface ≈ 1 tool / ~1,044 tokens | `packages/sdk/src/execute.test.ts` | ✅ pinned |
| §9.2 — a secret never enters sandbox heap / agent code / agent / model | `packages/sdk/src/credentials.test.ts` | ✅ pinned |
| §9.3 — loopback/private egress off by default (authoritative check: per-connect IP pinning, `createPinnedLookup` — canonicalize-then-check, closes DNS-rebinding TOCTOU per spec §18) | `packages/sdk/src/pipeline/egress.test.ts` (redirect refusal covered in `pipeline/upstream.test.ts`) | ✅ pinned |
| §11 — Trace stores no raw credentials; inputs redacted per policy (write-time; full `output` dropped from Trace — replay journal deliberately unredacted per §5.5 D7) | `packages/sdk/src/pipeline/invoker.test.ts` (+ D7 guard in `execution/manager.test.ts`) | ✅ pinned |
| §16 — runaway executions interrupted (time / memory / output caps); a pathologically deep guest value (parse OR return) fails cleanly without crashing the host (gate-two finding #1) | `packages/sdk/src/sandbox/quickjs.test.ts` | ✅ pinned |
| §16 — a host-stack overflow cannot poison the shared QuickJS module: it is detected and the module is rebuilt, so repeated pathological executions never strand a later one (sequential, cross-instance, concurrent, bounded-recovery), while only a host-stack overflow is classified poison-capable — other host faults stay operator-visible infra faults (gate-two DoS finding #3) | `packages/sdk/src/sandbox/quickjs.test.ts` | ✅ pinned |
| §16 — the wall-clock budget is wired into the invoker: `makeInvoker` receives a `deadline` reflecting the remaining budget, so per-call timeouts clamp to `min(ceiling, remaining)` end-to-end and the budget-exhausted refusal is live (gate-two finding F1) | `packages/sdk/src/execution/manager.test.ts` (end-to-end clamp + `min(ceiling, deadline())` unit in `packages/sdk/src/pipeline/invoker.test.ts`) | ✅ pinned |
| §5.5 — pause/resume via deterministic replay (journaled results, seeded non-determinism) | `packages/sdk/src/execution/manager.test.ts` | ✅ pinned |
| §10.2 — policy defaults: safe→Allow, review/destructive→Require approval; block never seeded | `packages/sdk/src/policy.test.ts` | ✅ pinned |
| /mcp M1 — the human-only approval seam: no resume/approve tool on the MCP surface | `packages/mcp/src/server.test.ts` | ✅ pinned |
| /mcp M8 — stdout purity: every stdout byte the client transport did NOT consume is protocol-framed | `packages/mcp/src/integration.test.ts` | ✅ pinned |
| §4.2 — `check_execution` definition stays ≤ 256 estimated tokens (the M1 design's status-check counterpart to the execute token pin above) | `packages/mcp/src/payloads.test.ts` | ✅ pinned |
| /mcp M4 — outcome persistence: a stored `failed` row always explains itself (synthetic `ConduitPersistError` fallback on a faulted settle write) | `packages/sdk/src/execution/manager.test.ts` (credential-echo persisted-outcome facet covered in `packages/sdk/src/e2e.smoke.test.ts` — "runs the whole prototype flow with no secret leakage", Phase 11) | ✅ pinned |
| §4.2 — execute surface stays within the one-tool token budget (~1,044) with a capped connection listing | `packages/sdk/src/execute.test.ts` | ✅ pinned |
| /mcp CLI — approvals queue lists paused executions oldest-first (deterministic (started_at, id)) | `packages/sdk/src/store/sqlite.test.ts` | ✅ pinned |
| /mcp CLI — add-mcp provisioning is atomic (source/integration/connection/tools, plus an optional `removeSecretRef` secret DELETE for the `--clear-credential` path — T-I2 amendment) + seeds NO policy rows | `packages/sdk/src/store/sqlite.test.ts` | ✅ pinned |
| /mcp M8 — stdout purity holds through `conduit serve` (shared runStdioServer) | `packages/cli/src/integration.test.ts` | ✅ pinned |
| /cli add-mcp — an unreachable/dead upstream fails loud and writes 0 rows (the fetch moved DAEMON-side in Task 8; the test moved with it) | `packages/mcp/src/daemon/provision.test.ts` ("INVARIANT /cli add-mcp: unreachable --url fails loud and writes nothing") + `packages/cli/src/integration.test.ts` ("INVARIANT /cli add-mcp: a dead url exits non-zero and writes 0 rows") | ✅ pinned |
| /cli add-mcp — the credential is never echoed to stdout/stderr on any path (success, warn) | `packages/mcp/src/daemon/provision.test.ts` ("INVARIANT /cli add-mcp: secret is never echoed to stdout or stderr on a successful add" + the `--replace` warning-path variant) + `packages/cli/src/add-mcp.test.ts` ("INVARIANT §17: the CLI never receives a credential — no daemon response field carries one, and the secret is never echoed") | ✅ pinned |
| /cli add-mcp — re-sync with no `CONDUIT_ADD_SECRET` preserves an existing `credentialRef` (read-then-rewrite, never NULLed) | `packages/mcp/src/daemon/provision.test.ts` ("re-sync with no CONDUIT_ADD_SECRET but an existing credentialRef preserves it") | ✅ pinned |
| §18-C4 D2 — retarget credential-leak guard: a `--replace` to a NEW url while a stored credential exists is REFUSED (0 writes, old cred intact) unless a fresh `CONDUIT_ADD_SECRET` or `--clear-credential` is supplied; the stored secret is bound to the old host and reused for the onboarding fetch ONLY when the url is unchanged, never sent to a different one. Since Task 8 this guard runs DAEMON-side, where the credential lives | `packages/mcp/src/daemon/provision.test.ts` ("INVARIANT /cli add-mcp: refuses --replace retarget to a new url while a stored credential exists (no env, no clear) — 0 writes, old cred intact") | ✅ pinned |
| §9.3 — `createApprovalRuntime`'s egress wiring, pinned DIRECTLY at the seam both callers (mcp server + cli approvals) share, not just transitively | `packages/mcp/src/runtime.test.ts` ("INVARIANT §9.3: allowPrivateEgress:false BLOCKS a loopback upstream call without naming the env var" + the `true` opt-in counterpart) | ✅ pinned |
| /cli approvals — `approvals list` is read-only: expiry is presentation, computed at display time from `pausedOn.expiresAt`, never a write | `packages/cli/src/approvals.test.ts` ("INVARIANT /cli approvals: list renders paused rows oldest-first, with EXPIRED and time-remaining labels, and performs no write") | ✅ pinned |
| §18-C4 — MCP client rejects an out-of-allowlist protocol version counter-offer, naming both the offered value and the supported set (versions are exactly `["2025-06-18","2025-03-26"]`, newest sent first) | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: rejects a counter-offer outside the allowlist, naming both sides") | ✅ pinned |
| §18-C4 — MCP client validates the `Mcp-Session-Id` header: empty / whitespace / non-visible-ASCII rejected as a protocol error, visible-ASCII (incl. boundary chars) accepted, absent → sessionless, and a repeated (array-valued) header is malformed | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: Mcp-Session-Id validation matrix — empty and non-visible-ASCII rejected; visible-ASCII boundary accepted" + "INVARIANT §18-C4: a repeated (array) Mcp-Session-Id header is malformed and rejected") | ✅ pinned |
| §18-C4 — handshake responses are capped OFF THE WIRE (never content-length), and the byte budget is CUMULATIVE across every response in the operation (a shared `bytesLeft`, stronger than upstream.ts's per-response cap) → `cap` error on breach | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: handshake responses are capped — a huge initialize body is a cap error, and the budget is cumulative") | ✅ pinned |
| §18-C4 — the `notifications/initialized` post must land as exactly HTTP 202 with a zero-byte body, else the handshake fails as a protocol error | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: initialized must be exactly 202 with an empty body") | ✅ pinned |
| §18-C4 — `tools/list` follows `nextCursor` to completion and the tool-count cap spans ALL pages (not reset per page) | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: tools/list follows nextCursor to completion and the tool-count cap spans ALL pages" + the maxTools-cap variant + "the byte budget is cumulative across pages") | ✅ pinned |
| §18-C4 — the scoped 404-session-expiry retry fires ONLY when the failed request carried a session id, and at most ONCE per logical operation (a counter in the operation frame, not per page); a sessionless 404 or a second consecutive 404 surfaces immediately | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: 404 retry fires ONLY when the request carried a session id, at most once" + the second-404 and sessionless variants) | ✅ pinned |
| §18-C4 (design D3) — a delayed 404 from an old session generation cannot invalidate a session already renewed by someone else: the retry's re-initialize publishes to the caller's session in place only if the caller's sessionId still strictly equals the id that 404'd | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: a delayed 404 from an old session cannot invalidate a newly established session") | ✅ pinned |
| §18-C4 — a 404 mid-pagination restarts `tools/list` from page one (stale cursor discarded, accumulated tools discarded), while the cumulative byte budget is NOT reset | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: a 404 mid-pagination restarts from page one, discarding the stale cursor, keeping the cumulative budget") | ✅ pinned |
| §18-C4 — a server ping received mid-stream (during `tools/call`) is answered inline and the real response still completes | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: a server ping mid-stream is answered and the response still arrives") | ✅ pinned |
| §18-C4 — the best-effort session `DELETE` is deadline-bounded on the body-drain phase too: a server that sends DELETE headers but never ends the body cannot outlive the budget — the drain deadline destroys the socket and rejects `timeout` (callers swallow it), so one broken/malicious upstream cannot hang every execution's `dispose()` | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: deleteSession is deadline-bounded — a never-ending DELETE body cannot outlive the budget") | ✅ pinned |
| §18-C4 — an SSE response is read INCREMENTALLY and reading STOPS at the matching JSON-RPC id (design D2 early-stop): a server that delivers the response but holds the stream open does not turn a delivered success into a budget timeout | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: reading stops at the matching response — a held-open SSE stream does not time out a delivered result") | ✅ pinned |
| §18-C4 — a mid-stream `ping` is answered LIVE over its own POST while the SSE stream is still open, so a server that waits for its ping answer before sending the response does not deadlock until timeout | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: a mid-stream ping is answered while the stream is still open") | ✅ pinned |
| §18-C4 — a non-2xx response classifies `http_status` REGARDLESS of its content-type: a 404 dressed in `text/event-stream` still triggers the session-expiry retry, and a 401/403 still reaches the credential-error mapping — never streamed to no-match and misfiled as `protocol` | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: a non-2xx response with an SSE content-type still classifies as http_status — a 404 dressed as SSE triggers the session-expiry retry" + the 401-with-SSE variant) | ✅ pinned |
| §18-C4 — a JSON-RPC response carrying NEITHER `result` NOR `error` is a protocol violation ("carried neither a result nor an error"), never a `{result: null}` success the caller could memoize (presence tested with `in`, since JSON cannot encode undefined) | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: a response with neither result nor error is a protocol violation, never a null success") + `packages/sdk/src/pipeline/mcp-wire.test.ts` ("INVARIANT §18-C4: a matched id with neither result nor error is a protocol violation, not a response") | ✅ pinned |
| §18-C4 / F-1 — the cumulative byte allowance is SHARED across every client built from one budget: serve-time passes ONE budget to the cached handshake client AND the fresh per-call client, so a handshake body + a call body that together exceed `maxBytes` fail `cap` (the first call cannot get two full budgets) | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: the byte budget is cumulative across the handshake and the call in one operation (shared across clients)") | ✅ pinned |
| §18-C4 / F-2 — the pre-response (connect + headers) phase is bounded by the ABSOLUTE remaining deadline via a wall-clock timer, not socket-inactivity: a slowloris that dribbles header bytes cannot evade the budget | `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: the pre-response phase is bounded by the ABSOLUTE deadline, not socket inactivity (slowloris)") | ✅ pinned |
| §18-C4 / F-7 — a hostile null (or non-object) JSON-RPC `error` member is NOT a valid error member: with no result it is a malformed envelope (protocol error), never read as `error.message`/`.code` off null (which would TypeError and be mislabeled a network error) — protects initialize/listTools/callTool uniformly at `classifyOne` | `packages/sdk/src/pipeline/mcp-wire.test.ts` ("INVARIANT §18-C4: a null error member with no result is a malformed envelope, not a response") + `packages/sdk/src/pipeline/mcp-client.test.ts` ("INVARIANT §18-C4: a hostile null error member on initialize is a protocol error, not a TypeError") | ✅ pinned |
| §18-C4 / F-6 — the SSE frame parser conforms to WHATWG line-ending rules: splits on CR, LF, and CRLF, strips one optional leading U+FEFF BOM at stream start, and DEFERS a chunk-trailing lone CR so a CRLF split across two chunks frames as ONE event (byte-exact framing regardless of chunk boundaries) | `packages/sdk/src/pipeline/mcp-wire.test.ts` ("INVARIANT §18-C4: splits on CR-only line endings (WHATWG SSE)" + "INVARIANT §18-C4: strips a single leading BOM at stream start" + "INVARIANT §18-C4: a CRLF split across chunks frames as one event") | ✅ pinned |
| §6 / §18-C4 — the whole start()/resume() prep window (running-state persist → drive takeover) terminalizes the `running` row to `failed` on ANY throw — `makeUpstreamSession()`, `deadlineFor()`, AND `makeInvoker()` included — never stranded `running` (running must reach a terminal); `resume()` guards the same window via `failClaimedResume` | `packages/sdk/src/execution/manager.test.ts` ("INVARIANT §18-C4: a throwing makeUpstreamSession at start() terminalizes the row failed, never stranded running" + "INVARIANT §6: a synchronously-throwing makeInvoker at start() terminalizes the row failed, never stranded running" + "INVARIANT §6: a throwing makeUpstreamSession on the RESUME path terminalizes, never stranded running (F-5 twin)") | ✅ pinned |
| §18-C4 — the execution manager owns a per-drive upstream session scope: created before `makeInvoker`, disposed in a `finally` on EVERY drive exit (success, failure, pause) | `packages/sdk/src/execution/manager.test.ts` ("INVARIANT §18-C4: the manager disposes the upstream session scope on every drive exit — success, failure, AND pause") | ✅ pinned |
| §18-C4 — a resumed drive gets a FRESH upstream session scope, never a reuse of the paused drive's scope | `packages/sdk/src/execution/manager.test.ts` ("INVARIANT §18-C4: a resumed drive gets a FRESH scope") | ✅ pinned |
| §18-C4 — per-drive upstream session scope: auth material is never stored (only salted HMAC digest); cache key rotation on secret change; cache key canonical on mixed-case header names | `packages/sdk/src/pipeline/upstream-session.test.ts` ("INVARIANT §18-C4: same-url same-ref secret rotation forces a new session (auth digest key)" + "mixed-case header names produce the same cache key as lowercase") | ✅ pinned |
| §18-C4 — single-flight semantics: concurrent first acquires share one `make()` call; a rejected make is not cached so the next acquire retries | `packages/sdk/src/pipeline/upstream-session.test.ts` ("caches by url+auth and single-flights concurrent first acquires" + "a failed make is not cached (next acquire retries)") | ✅ pinned |
| §18-C4 — `UpstreamSessionScope.dispose()` NEVER throws; all cached sessions are deleted best-effort, with exceptions logged but never re-raised; a second dispose is a no-op | `packages/sdk/src/pipeline/upstream-session.test.ts` ("INVARIANT §18-C4: dispose deletes every cached session and never throws" + "dispose swallows a throwing deleteSession into the log") | ✅ pinned |
| §18-C5 — `normalizeMcp` records the raw upstream wire name in `sourceSemantics.upstreamName` so hyphenated names (e.g. `resolve-library-id`) round-trip correctly at serve time | `packages/sdk/src/normalize/mcp.test.ts` ("INVARIANT §18-C5: records the raw upstream name so hyphenated names round-trip") | ✅ pinned |
| §18-C5 — the stored `upstreamName` survives the full normalize → sqlite store → hydrate → serve-time round-trip and lands VERBATIM on the wire (`tools/call` `params.name`), so a hyphenated name is never sent as its underscore-rewritten qualified name; a legacy row whose stored `source_semantics` lacks `upstreamName` falls back to the A5 prefix-strip | `packages/sdk/src/pipeline/upstream.test.ts` ("INVARIANT §18-C5: the stored upstream name is sent on the wire" — "sends a hyphenated upstreamName from sourceSemantics, hydrated through the store" + "falls back to prefix-strip for a legacy row whose source_semantics lacks upstreamName") | ✅ pinned |
| §18-C4 / F1 — the serve-time caller's WHOLE logical operation (streamable-HTTP handshake + `tools/call` + one 404-retry) shares the single `request.timeoutMs` deadline: a handshake that stalls past the budget times out as a `ConduitUpstreamError` ("timed out after {ms}ms") and no `tools/call` is ever sent | `packages/sdk/src/pipeline/upstream.test.ts` ("INVARIANT F1: handshake + call share request.timeoutMs" — "times out during the handshake before any tools/call is sent") | ✅ pinned |
| §18-C4 / F1 — chained calls in ONE drive that reuse a cached session each get a FRESH per-call budget: the caller constructs a fresh MCP client per `call()` (design D3 caches session state, not the client), so call #2+ runs under its own deadline and its own cumulative byte allowance — a call that starts after call #1's budget window elapsed still succeeds, and two responses that each fit but together exceed one 1-MiB budget do not spuriously `cap` | `packages/sdk/src/pipeline/upstream.test.ts` ("INVARIANT §18-C4: chained calls in one drive each get a fresh per-call budget while reusing the session") | ✅ pinned |
| §18-C4 / F1 — the wait on a single-flighted session acquire is bounded by the WAITING call's own operation deadline: a call piggybacking on a peer's in-flight handshake (or a hostile upstream stalling that handshake) cannot block past its own `timeoutMs`; a cache hit resolves immediately | `packages/sdk/src/pipeline/upstream.test.ts` ("INVARIANT §18-C4: waiting on a peer's in-flight handshake is bounded by the waiter's own deadline") | ✅ pinned |
| §5.5 D6 — `resume` reports `decisionApplied`: host-side truth that the staged decision was CONSUMED by the pending call (the decisions seam's one-shot `take`), independent of drive status and never inferable from guest-forgeable error names — an applied deny is true even when the guest catches it and completes; a guest-spoofed `ConduitPolicyBlocked` failure with no consumption stays false | `packages/sdk/src/execution/manager.test.ts` ("INVARIANT §5.5: an applied approve reports decisionApplied:true" + the applied-deny completed/failed variants + "INVARIANT §5.5: a guest-spoofed ConduitPolicyBlocked failure does NOT read as an applied decision") | ✅ pinned |
| /cli approvals — deny verb-truth: `approvals deny` reports the OPERATOR'S verb from `decisionApplied`, never the outcome's error name — an applied deny prints `denied`/exit 0 whatever the drive then did (guest-caught completion, later unrelated failure, re-pause), and a deny that never applied (incl. a guest-spoofed `ConduitPolicyBlocked`) exits 1 and is never labeled `denied` | `packages/cli/src/approvals.test.ts` ("INVARIANT /cli deny-verb-truth: …" — real-runtime applied deny + applied-deny completed/failed/spoof/not-applied variants) | ✅ pinned |
| §16.3 — wrong master key fails at store open (canary), not first use | `packages/sdk/src/store/key-lifecycle.test.ts` ("INVARIANT §16.3: wrong master key fails at store open (canary), not first use") | ✅ pinned |
| §16.3 — legacy db is never bound to an unverified key (probe-before-bootstrap); one corrupt row cannot condemn a correct key (probe-all); canary corruption is distinguished from wrong key | `packages/sdk/src/store/key-lifecycle.test.ts` ("INVARIANT §16.3: legacy db (real rows, no canary) + wrong key → refused, canary NOT created" + "INVARIANT §16.3: legacy db + correct key → canary created, subsequent opens pass" + "INVARIANT §16.3: corrupted canary + healthy real row → canary-corruption error, not wrong-key" + "INVARIANT §16.3: probe-all — corrupt FIRST real row + later good row → still canary-corruption" + "INVARIANT §16.3: canary fails + ALL real rows fail → ambiguous wrong-key-or-corruption error") | ✅ pinned |
| §16.3 — rotation re-seal is atomic: a mid-rotate failure leaves every secret under the OLD key (confirmed via rollback); after success every row (canary incl.) opens ONLY under the new key | `packages/sdk/src/store/key-lifecycle.test.ts` ("INVARIANT §16.3: after re-seal, every row (canary incl.) opens under the NEW key, none under the old" + "INVARIANT §16.3: atomic — a mid-rotate failure leaves every row openable under the OLD key") | ✅ pinned |
| §16.3 — commit-boundary honesty: an uncertain COMMIT/ROLLBACK outcome is classified "unknown" (caller must not delete either candidate key) — classifier pin plus END-TO-END wiring through a real proxy-client commit()/rollback() failure | `packages/sdk/src/store/key-lifecycle.test.ts` ("classifyReencryptFailure maps phases to db states (classifier pin)" + "INVARIANT §16.3: a throwing commit() drives reencryptSecrets to dbState 'unknown'" + "INVARIANT §16.3: a throwing rollback() (after a mid-loop failure) drives reencryptSecrets to dbState 'unknown'") + `packages/cli/src/key.test.ts` ("INVARIANT §16.3: an UNCERTAIN reencrypt outcome (commit throws) exits 1, PRESERVES master-key.next, and names UNCERTAIN + recovery") | ✅ pinned |
| §16.3 — key resolution: env overrides file; neither → loud error naming `conduit key generate`; wide key-file perms warn-and-serve | `packages/mcp/src/env.test.ts` ("INVARIANT §16.3: env var wins over the key file; keySource says so" + "INVARIANT §16.3: no env var → key file is read, validated, keySource 'file'" + "INVARIANT §16.3: neither env nor file → loud error naming `conduit key generate`" + "INVARIANT §16.3: key file wider than 0600 → stderr warning, still serves") | ✅ pinned |
| §16.3 — conduit.db (and sidecars) 0600 at creation via the env open path; wider existing perms healed; a real store open + write leaves any materialized WAL sidecars 0600 | `packages/mcp/src/env.test.ts` ("INVARIANT §16.3: db file created 0600 before libsql touches it; dir 0700" + "INVARIANT §16.3: existing wider-perms db and sidecars healed to 0600" + "INVARIANT §16.3: a real store open + write leaves any materialized WAL sidecars 0600") | ✅ pinned |
| §16.3 — `key generate` refusals (file exists / env set / db has sealed rows) and 0600 link-publication; key material never printed | `packages/cli/src/key.test.ts` ("INVARIANT §16.3: happy path — 0600 key file, no key material on stdout/stderr" + "INVARIANT §16.3: refuses when the key file exists (points at rotate)" + "INVARIANT §16.3: refuses when CONDUIT_MASTER_KEY is set" + "INVARIANT §16.3: refuses when the default db already holds sealed rows") | ✅ pinned |
| §16.3 — `key rotate` refusals (env-sourced key / custom CONDUIT_DB / held write lock / leftover master-key.next); a BUSY refusal removes its own `.next` so the retry is not poisoned; `.next` is claimed (wx) BEFORE `.bak` is touched, so an EEXIST loser never copies `.bak` and is NEVER told to delete a possibly-live `.next` | `packages/cli/src/key.test.ts` ("INVARIANT §16.3: refuses an env-sourced key" + "INVARIANT §16.3: refuses a custom CONDUIT_DB path" + "INVARIANT §16.3: refuses a leftover master-key.next (never silently reused or deleted)" + "INVARIANT §16.3: BUSY refusal removes its own .next — retry is not poisoned" + "INVARIANT §16.3: reencrypt-acquire BUSY removes the already-written .next — retry not poisoned" + "INVARIANT §16.3: EEXIST on the .next claim (concurrent/crashed rotation) refuses without ever advising deletion, and never touches .bak") | ✅ pinned |
| §16.3 — rotation end-state: db + key file both new, `master-key.bak` = old, real secret round-trips; crash-table row-2 manual promotion recovers; key material never printed; a pre-transaction fs failure AFTER `.next` is claimed (e.g. the `.bak` copy) self-cleans the now-orphaned `.next` and names the way forward without touching the db | `packages/cli/src/key.test.ts` ("INVARIANT §16.3: end state — db + key file both new, .bak = old, secret still opens; keys never printed" + "INVARIANT §16.3: crash-table row 2 — .next promotes manually and the db opens" + "INVARIANT §16.3: a pre-transaction fs failure (backup/stage) names the way forward, db untouched, .next self-cleaned") | ✅ pinned |
| §16.3 — key persistence never trusts a single `writeSync` call: a short (partial) write is looped until every byte is down before fsync, for both `generate`'s temp key and `rotate`'s `.next` key, via one shared helper | `packages/cli/src/commands/key-fs.test.ts` ("writes the full buffer and fsyncs, on a normal single-syscall write" + "INVARIANT §16.3: loops until every byte is written when writeSync returns a short count") | ✅ pinned |
| §16.3 — the key canary row (`CANARY_REF`) cannot be overwritten or deleted through the `secrets` repository OR through `provisionSource`'s direct secrets upsert/delete — only the key-lifecycle module's own raw-SQL access may touch it | `packages/sdk/src/store/sqlite.test.ts` ("INVARIANT §16.3: put refuses the canary ref — a colliding put cannot corrupt the canary" + "INVARIANT §16.3: remove refuses the canary ref — the canary row survives" + "INVARIANT §16.3: provisionSource refuses secret.ref === CANARY_REF — canary intact" + "INVARIANT §16.3: provisionSource refuses removeSecretRef === CANARY_REF — canary intact") | ✅ pinned |
| §17 — the SQLite lock-db primitive: a SHARED holder blocks a real cross-process EXCLUSIVE acquire, and the lock releases once the holder is SIGKILLed | `packages/mcp/src/daemon/locks.test.ts` ("INVARIANT §17: lock-db-shared-blocks-exclusive-through-libsql") | ✅ pinned |
| §17 — detection always probes SHARED (an EXCLUSIVE probe cannot distinguish holders): a SHARED holder still reads `probeShared` = "free", while an EXCLUSIVE holder reads "busy" | `packages/mcp/src/daemon/locks.test.ts` ("INVARIANT §17: lock-db-probe-distinguishes-shared-and-exclusive") | ✅ pinned |
| §17 — a hold is a live open transaction, not a one-shot statement: it keeps blocking a fresh acquire well after the holder printed ready, with no further action from the holder | `packages/mcp/src/daemon/locks.test.ts` ("INVARIANT §17: lock-db-transaction-remains-held-until-explicit-close") | ✅ pinned |
| §17 — an unrelated sibling client opening and closing against the same lock-db file does not disturb a lock held by another connection | `packages/mcp/src/daemon/locks.test.ts` ("INVARIANT §17: lock-db-sibling-connection-close-does-not-release-lock") | ✅ pinned |
| §17 — the lock is fcntl-backed and releases on process death even while an orphaned descendant of the killed holder keeps running (SQLite's close-on-exec fds, not the close-drops-locks POSIX footgun) | `packages/mcp/src/daemon/locks.test.ts` ("INVARIANT §17: lock-db-releases-after-sigkill-with-orphan-child-alive") | ✅ pinned |
| §17 — singleton enforcement: two daemons racing to auto-start against one state directory produce exactly one server; the loser exits "already running" rather than binding a second endpoint over the same database | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: exactly one daemon survives a concurrent auto-start race") | ✅ pinned |
| §17 — the split-brain guard: a failed connect (or a refused second daemon) never unlinks a live listener; only the lifecycle-lock holder removes the endpoint, and only if device+inode still match what it bound | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: a failed connect to a LIVE listener never unlinks it") | ✅ pinned |
| §17 — kernel-enforced stop-first, both orders: rotation's EXCLUSIVE maintenance acquire is refused while a daemon holds it SHARED (and a second reader still gets in, proving the hold is shared), and a daemon starting during rotation exits "rotation in progress" having released the lifecycle lock it took first | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: rotation's exclusive maintenance acquisition blocks while a daemon holds it shared") | ✅ pinned |
| §17 — the readiness gate: a connection arriving during listener close never receives READY, so the client writes nothing and its later retry is a first attempt rather than a replay | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: a connection queued during listener close gets no READY and the client writes nothing") | ✅ pinned |
| §17 — a signal-stopped daemon drains without stranding work: it exits 0, releases both locks and removes its endpoint, and a successor started against the same state directory reads back the same durable approval state | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: a signal-stopped daemon exits with no paused work stranded") | ✅ pinned |
| §17 / §9.3 — the daemon is default-paths-only: a client whose env sets `CONDUIT_DB` gets the typed `refused-custom-db` handshake refusal instead of being served against a database it did not choose | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: a client whose env sets CONDUIT_DB is refused at handshake") | ✅ pinned |
| §17 — the crash-terminal sweep runs at its fixed point in the startup order: after both locks are held and before the endpoint is bound, so no client can observe a half-swept database | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: the crash-terminal sweep runs before the endpoint is bound") | ✅ pinned |
| §17 / §3.3 — a connection's capability is negotiated exactly once: since `handshake` is a member of every capability set, a second handshake claiming a wider role is refused and the previously-denied request stays denied (no re-handshake privilege escalation) | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: a second handshake cannot widen an established capability") | ✅ pinned |
| §17 — the crash-terminal sweep terminalizes what a dead daemon stranded, and touches nothing else: every durably-`running` row becomes `failed` carrying an ambiguous-outcome cause, while `paused`/`completed`/`failed`/`expired` rows are left exactly as they were | `packages/mcp/src/daemon/sweep.test.ts` ("INVARIANT §17: a running execution left by a killed daemon is terminalized, and nothing else is touched") | ✅ pinned |
| §17 — the sweep NEVER replays: a swept row's code is not re-executed and no result is invented for it, because a killed execution's upstream calls may already have landed | `packages/mcp/src/daemon/sweep.test.ts` ("INVARIANT §17: the sweep never replays — the swept row's code is not re-executed and its result stays absent") | ✅ pinned |
| §17 — end-to-end crash recovery with real processes: an execution left `running` by a SIGKILLed daemon reaches a defined terminal state (`failed`, ambiguous-outcome cause, no invented result) on restart, and a seeded non-zero audit-trail count for that execution is unchanged across the restart — a replay would re-enter the pipeline and append rows | `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: a killed daemon's running execution is terminalized on restart and never re-runs") | ✅ pinned |
| §17 / §5 — the retry rule: a daemon killed AFTER the request bytes are written yields `outcome-unknown` and the client never retries, so an execution that may already have run is never duplicated | `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: a daemon killed after the request is written yields outcome-unknown, never a retry") | ✅ pinned |
| §17 / §3.5 — auto-start: a client that finds no daemon and no rotation spawns one and completes its request against the daemon it started | `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: a client with no daemon auto-starts and completes its request") | ✅ pinned |
| §17 / §3.5 — rotation is fail-fast: a client observing maintenance held EXCLUSIVE refuses immediately with a distinct `rotation-in-progress` code and spawns NO daemon, rather than spinning against the rotation it must let finish | `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: a client fails fast during rotation and never spawns a daemon") | ✅ pinned |
| §17 / §3.5 — the rotation WINDOW closes cleanly: a request refused with `rotation-in-progress` spends nothing, so once rotation releases the maintenance lock the very next request auto-starts a daemon and completes normally. Both halves are pinned in one test because neither alone proves it — a policy that consumed the spawn budget during the refused window would still pass the fail-fast half | `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: a request refused during rotation leaves the spawn budget intact for the next one" — real EXCLUSIVE maintenance hold, released mid-test, real spawned daemon after) | ✅ pinned |
| §17 / §3.5 — the spawn budget is ONE, independently of how long the deadline allows the decision table to be re-entered. Re-entry is bounded by TIME (the caller's `deadlineMs`) rather than by a fixed pass count, so the once-only guard must be unconditional rather than incidentally capped by the loop bound: with an injected spawn that starts nothing, every re-entry lands on decision-table row 4 again — the shape that becomes a fork bomb if the guard is anything less | `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: the spawn budget is ONE regardless of how long the deadline allows re-entry" — 15s deadline, dozens of passes, exactly one spawn) | ✅ pinned |
| §17 / §3.5 — a lock CONTENTION answer is never a raw throw: `probeShared`/`acquireShared` return "busy" for `SQLITE_BUSY` **and** `SQLITE_LOCKED`, whether libsql raises them as a `LibsqlError` or (as it does from the cross-process schema read) a PLAIN `Error`, and whether the shape carries the textual `code` or only the numeric `rawCode` (5/6). Classification is by CODE, never by error class — the retired `instanceof LibsqlError` guard let genuine contention escape as a raw throw, killing a client polling a daemon through its startup window and, on the row-1 maintenance probe, surfacing as a spurious `rotation-in-progress` refusal. Widened, not loosened: a non-contention fault (e.g. `SQLITE_CORRUPT`) still throws | `packages/mcp/src/daemon/locks-classify.test.ts` ("INVARIANT §17: a BUSY raised as a PLAIN Error is a lock answer, never a raw throw" + "INVARIANT §17: a LOCKED raised as a PLAIN Error is a lock answer, never a raw throw" + "classifies on the numeric rawCode when the textual code is absent" + "a NON-busy fault still throws — the classifier widened, it did not swallow") | ✅ pinned |
| §17 / §3.2 — `probeShared` reads "free" on `SQLITE_CANTOPEN` ONLY when the lock db is PROVABLY absent. Absence is decided by `statSync` errnos, not `existsSync`: only a literal `ENOENT` proves absence, while `EACCES` means "could not determine" and still throws — otherwise a permission-denied lock db reads as free and a client spawns a daemon against a state directory it cannot use. Covers the fresh install (nothing exists → free), the TOCTOU that caused the flake (the state directory created BY the daemon during the failed open → still free, because the lock db file itself is absent), and both permission cases (unreadable lock db, untraversable parent → throw) | `packages/mcp/src/daemon/locks-classify.test.ts` ("INVARIANT §17: a fresh install (nothing exists) reads free" + "INVARIANT §17: a directory created DURING the failed open still reads free (the TOCTOU)" + "INVARIANT §17: an existing but UNREADABLE lock db throws — never free" + "INVARIANT §17: an UNTRAVERSABLE parent directory throws — never free") | ✅ pinned |
| §17 / §3.1 — the spawn boundary: the daemon's environment is CONSTRUCTED, not inherited — hostile client `HOME`, `PATH`, and every `CONDUIT_*` value are absent from the child, whose only variable is the daemon-owned `PATH` constant; the executable is resolved absolutely from the running package, never through `PATH` | `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: hostile client HOME/PATH/CONDUIT_* values are inert through auto-start") | ✅ pinned |
| §17 / §3.1 — a failing unit of queued work is a typed error, never a dead daemon: the queue's run closure is invoked as `void dispatch(entry)`, so both a rejecting `manager.start` (store fault) and a legal-but-oversize result (the sandbox's default `maxOutputBytes` EQUALS `FRAME_CAP`, so an ordinary max-size envelope overflows) are caught inside the closure — the client gets `internal` / a "too large for the IPC frame" refusal and the daemon keeps serving every other client | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: a rejecting execution is a typed error, never a dead daemon" + "INVARIANT §17: a result too large for one IPC frame is a typed error, never a dead daemon") | ✅ pinned |
| §17 / §3.2 — fresh install is not a dead end: the daemon starts against a state directory that does not exist yet, creating it 0700 and then running the FULL boundary validation over it (mkdir-then-assert — creation is never a shortcut past the check), and a client on a nonexistent state directory reads both probes as "free", lands on decision-table row 4, and auto-starts | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: the daemon starts against a state directory that does not exist yet") + `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: a client auto-starts against a state directory that does not exist yet") + `packages/mcp/src/daemon/locks.test.ts` ("probeShared: a lock db whose directory does not exist reads free, and is not created") | ✅ pinned |
| §17 / §3.5 — a client waiting out a lifecycle-lock holder watches for EITHER outcome: a STARTING daemon holds the lock across store-open and sweep and resolves by BINDING, so the wait re-attempts `tryConnect` each tick and proceeds the moment the socket appears, rather than burning the whole deadline waiting for a release that a healthy start never performs | `packages/mcp/src/daemon/client.test.ts` ("INVARIANT §17: a client connects as soon as a STARTING daemon binds, without waiting out its deadline") | ✅ pinned |
| §17 / §3.5 — drain grace: a connection that has received READY counts as active work, so an idle READY-granted connection survives to the EXISTING `DRAIN_DEADLINE_MS` and can still complete a request issued during the grace window (it is ended at deadline expiry — the accepted residual of bounding the grace window rather than holding the lifecycle lock indefinitely); a connection that never received READY is ended immediately at drain start | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: an idle READY connection can still complete a request issued during drain grace") | ✅ pinned |
| §17 / §3.1 — `approvals.resume` is admitted through the SAME `ExecutionQueue` as `execute` (cap 4 / queue 16): resume re-enters sandbox execution by driving a paused execution's replay, so concurrent resumes queue rather than running unbounded past the cap in the one process the cap protects. Pinned at BOTH levels — the queue's own behavior, and the daemon's real `submitSandboxWork` dispatch wiring against a spawned daemon (cap filled by `execute`, a concurrent `approvals.resume` is admitted to the queue and never reaches the manager) | `packages/mcp/src/daemon/conduitd.test.ts` ("resume shares the SAME queue as execute — concurrent resumes queue rather than exceeding the cap" + "INVARIANT §17: approvals.resume is admitted through the SAME queue as execute — through the real dispatch path") | ✅ pinned |
| §17 / §3.3 — `execute.deadlineMs` must be a FINITE, non-negative number: `NaN` (never expires — every comparison false), a negative value (expires before dispatch) and `Infinity` (an unbounded queue entry) are all `typeof "number"` and each corrupts the admission bound differently, so the decoder rejects them rather than admitting a degenerate entry | `packages/mcp/src/daemon/rpc.test.ts` ("rejects degenerate execute.deadlineMs values that are still typeof number") | ✅ pinned |
| §17 / §3.3 — the D-B1 reads answer with PROJECTIONS, never repository rows: `catalog.listing` returns only `{prefix, label}` per connection plus a source count, so the `credentialRef` its source rows carry — and the secret it points at — appear in no byte the daemon sends; the execution lookups return the §M1 `CheckPayload` rather than the stored row | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17 / §3.3: the D-B1 reads answer with PROJECTIONS carrying no credential material") | ✅ pinned |
| §17 / §M1 — the CheckPayload projection is computed on the DAEMON's clock: a `paused` execution whose `pausedOn.expiresAt` has passed reads back as `expired` through `execution.get`, so the presentation decision belongs to the process that owns the row and a serve process with a skewed clock cannot present a live approval as dead (the serve client passes no clock at all — the `now` seam was removed in D-B1) | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: a paused execution past its TTL reads back as `expired` on the DAEMON's clock" — real spawned daemon, real 1s TTL, real elapsed wall time) | ✅ pinned |
| §17 / §5 — the client's per-request budget outlasts the daemon's worst LEGAL answer time for an `execute` (`EXECUTE_CLIENT_DEADLINE_MS > EXECUTE_ADMISSION_DEADLINE_MS + §16 wallClockMs`), so routine slowness cannot be reported as §5 ambiguity ("may have run, do NOT retry") and the daemon's strictly-better typed queue-expiry refusal is never lost to a client that abandoned first; the D-B1 reads carry their own shorter budget | `packages/mcp/src/server.test.ts` ("INVARIANT §17 / §5: the client deadline outlasts the daemon's worst legal execute") | ✅ pinned |
| §17 / §3.3 — D-B1 widened the `serve` capability row ALONE, and only with reads: the three new kinds are refused to `approvals` and `add-mcp`, and no administrative verb (`approvals.*`, `source.*`) is reachable from `serve` — pinned at the capability table AND at the consumer, where the whole MCP surface is driven against a daemon that throws on any kind outside the serve reads | `packages/mcp/src/daemon/rpc.test.ts` ("D-B1 widened serve alone — approvals and add-mcp gain nothing" + "INVARIANT §17 / §3.3: no administrative verb is reachable from serve") + `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17 / §3.3: the D-B1 reads are denied to every capability except serve") + `packages/mcp/src/server.test.ts` ("INVARIANT §17 / §3.3: the MCP surface reaches for NO administrative verb") | ✅ pinned |
| §17 / §M1 — `execute.requestKey` survives the daemon round trip and still suppresses duplicates: the key is persisted before the sandbox runs, so a reissue of the same key returns `conflict` rather than starting a second run against upstream side effects that already landed, and the original execution stays recoverable by that key | `packages/mcp/src/daemon/conduitd.test.ts` ("INVARIANT §17: execute.requestKey round-trips — the same key returns conflict, never a second run") | ✅ pinned |
| §17 / §5 — the ambiguity is reported, never retried: an `outcome-unknown` from the daemon surfaces to the agent as an explicitly UNKNOWN outcome naming its correlation id, telling it to look the execution up via `check_execution` and NOT to retry — because after the request bytes are written, "never ran" and "ran and the ack was lost" are indistinguishable and a replay is an unauthorized side effect | `packages/mcp/src/server.test.ts` ("INVARIANT §17 / §5: an outcome-unknown execute is reported as ambiguous and tells the agent NOT to retry") | ✅ pinned |
| §17 — a write made through ONE daemon client is visible to ANOTHER with no restart: an approval applied by a spawned `conduit approvals approve` (capability `approvals`) is seen by the ORIGINAL, still-connected `conduit serve` session (capability `serve`) on its very next `check_execution`. It holds because the daemon is the sole writer and the serve process caches nothing. Before Task 7 this was unobservable — `approvals` opened the database directly, so the daemon had to be stopped first | `packages/cli/src/integration.test.ts` ("INVARIANT §17: a write made through ONE daemon client is visible to ANOTHER with no restart — approvals resumes, the live serve session sees it" — real spawned CLI processes against one live daemon) | ✅ pinned |
| §17 / §3.3 — the `approvals.resume` response is a PROJECTION that deliberately carries `decisionApplied`: the operator's VERB truth is host-side (whether the staged decision was consumed), and it is unrecoverable from status or error name — an applied deny can present as `completed` and an unapplied one as a guest-forged `ConduitPolicyBlocked` failure. Widening the response of an existing kind is not a capability widening, and the projection still drops the raw `pausedOn.input` call arguments | `packages/mcp/src/payloads.test.ts` ("INVARIANT §17 / §3.3: the resume projection carries decisionApplied — the verb-truth axis the CLI keys on") | ✅ pinned |
| §17 / §5 — an `outcome-unknown` resume is NEVER reported as a landed verb: `conduit approvals` prints no "denied"/"approved" line, exits non-zero, and tells the operator to re-list rather than retry — a retried decision could apply a second time to an execution that already consumed one | `packages/cli/src/approvals.test.ts` ("INVARIANT §17 / §5: an outcome-unknown resume is NEVER reported as a landed verb, and exits non-zero") | ✅ pinned |
| §17 / §5 — `approvals.resume` carries a RUN-sized client budget, not a read's (`RESUME_CLIENT_DEADLINE_MS > RESUME_ADMISSION_DEADLINE_MS + §16 wallClockMs`): a resume is admitted through the same ExecutionQueue as `execute` and then DRIVES a replay, so a read-sized budget would abandon an approve merely queued behind the concurrency cap and report it as §5 ambiguity. `approvals.list` is a genuine read and keeps the short budget | `packages/mcp/src/server.test.ts` ("INVARIANT §17 / §5: `approvals.resume` carries a RUN-sized budget, not a read's — the same ordering constraint as execute") | ✅ pinned |
| §17 / §3.3 — `conduit approvals` reaches for NOTHING outside its capability row: driving `list`, `approve` and `deny` names exactly `approvals.list` and `approvals.resume` and no other kind, so the converted client cannot drift into a verb its role does not carry | `packages/cli/src/approvals.test.ts` ("INVARIANT §17 / §3.3: the command reaches for NOTHING outside the approvals capability row") | ✅ pinned |
| §17 / §3.3.1 — THE ANTI-ORACLE: a client naming a foreign destination cannot cause any stored credential to be sent there. `source.revalidate` carries ONLY a namespace — `decodeRequest`'s extra-key strictness makes a `url` field unrepresentable, not merely unused — and the daemon derives BOTH the url and the credential from the stored row, so a client able to name one half can never name the other. Pinned against a REAL spawned daemon with a hand-crafted frame written straight onto the socket (bypassing `add-mcp.ts`, as an attacker would) and a recording HTTP fixture at the foreign origin, which receives NO request and therefore no `Authorization` header. Verified non-vacuous: with the decoder's strictness removed, the fixture records the credential verbatim | `packages/mcp/src/daemon/source-invariants.test.ts` ("INVARIANT §17: a client naming a foreign destination cannot cause any stored credential to be sent there") + `packages/mcp/src/daemon/rpc.test.ts` ("rejects the anti-oracle shape: source.revalidate carrying a url field") | ✅ pinned |
| §17 / §3.3.1 — a cross-origin redirect never carries a stored credential: the shared MCP client refuses EVERY 3xx outright (`McpClientError` `http_status`) rather than following it with headers attached, which is stronger than §3.3.1's "drops the credential on a cross-origin hop" — the second origin is never contacted at all. Pinned with two recording fixtures: the operator-named origin 302s to a second one, the provision fails with 0 writes, and the second origin records no request and no `Authorization` header | `packages/mcp/src/daemon/source-invariants.test.ts` ("INVARIANT §17: a cross-origin redirect never carries a stored credential") | ✅ pinned |
| §17 / §9.2 — the secret is never echoed daemon→client, in ANY field: `source.provision` carries the operator's fresh secret client→daemon ONCE (the sole permitted direction — the operator is supplying their own data), and neither response nor any daemon log line contains it. The `ProvisionPayload` projection answers with counts and a two-valued credential PRESENCE flag, never a ref and never the value. Pinned by a whole-frame scan over both a provision and a revalidate (the path that has just read the plaintext out of the store), plus a daemon-log scan, with the upstream fixture confirming the credential was genuinely in play so the scan is not vacuous | `packages/mcp/src/daemon/source-invariants.test.ts` ("INVARIANT §17: no response to any add-mcp RPC echoes the secret, in any field") | ✅ pinned |
| §17 — a SOURCE added through ONE daemon client is visible to ANOTHER with no restart: a `source.provision` issued on an `add-mcp` connection is seen by the ORIGINAL, still-connected `serve` connection on its very next `catalog.listing` (sourceCount 0 → 1, the new prefix advertised). **This is the §17 startup-reload caveat closing** — the direct payoff of daemon ownership, and unbuildable until `source.provision` became real in Task 8 (Task 7 pinned only the approvals-shaped version) | `packages/mcp/src/daemon/source-invariants.test.ts` ("INVARIANT §17: a source added via one client is visible to another with no restart" — real spawned daemon, two live connections, no reconnect) | ✅ pinned |
| §17 / §5 — `source.provision` / `source.revalidate` carry a FETCH-sized client budget, not a read's (`PROVISION_CLIENT_DEADLINE_MS = ONBOARDING_DEADLINE_MS + READ_DEADLINE_MS`): these are the only kinds whose daemon-side work performs an outbound network fetch, so a read-sized budget would abandon an onboarding the daemon was still legitimately performing and report §5 ambiguity — and an ambiguous provision is worse than a slow one, since the operator cannot tell whether the atomic write landed. The onboarding bound is imported from `mcp-fetch.ts` rather than re-declared, so the two cannot drift | `packages/mcp/src/server.test.ts` ("INVARIANT §17 / §5: source.provision carries a FETCH-sized budget that outlasts the onboarding deadline") | ✅ pinned |
| §17 / §3.4 — rotation's exclusion is the KERNEL maintenance lock, not a liveness query, and it holds in BOTH orders: a live daemon (holding maintenance SHARED for its lifetime) makes `conduit key rotate` refuse non-blocking with nothing read or written — no `.next`, no `.bak`, key file untouched — and a held rotation makes a starting daemon exit `EXIT_ROTATION_IN_PROGRESS`, then start normally once released. This replaces the pre-Task-9 write-lock probe, which §3.4 rejects outright: a liveness check cannot close the window in which an unrelated client auto-starts a daemon between the check and the re-seal. `key generate` sits behind the same lock, because §2.1's second direct `createClient` (the sealed-row count) is inside the same boundary | `packages/cli/src/key.test.ts` ("INVARIANT §17 / §3.4: order A — a LIVE daemon makes rotate refuse non-blocking, naming the holder" + "INVARIANT §17 / §3.4: order B — a HELD rotation makes a starting daemon exit `rotation in progress`" + "INVARIANT §17 / §3.4: `key generate` is behind the same lock — its db inspection cannot race a daemon" — real spawned daemon processes on both sides) | ✅ pinned |
| §17 / §3.4 — a refusal NAMES its holder: the maintenance lock db carries a diagnostic `lock_holder` row (`pid`, `role`, `since`) stamped BEFORE the acquiring transaction opens, so a rotate refused by a live daemon reports "Last acquired by daemon (pid N) at … (may be stale)" instead of sending the operator hunting. The row is advisory only — nothing branches on it, the kernel lock is the exclusion (§3.4: "the registry may still report metadata for a good error message, but the lock provides the exclusion") — and it lives in the LOCK db, never the product db. Readable past a SHARED holder by construction; an EXCLUSIVE holder blocks the read, which is why it is only ever read as a companion to a BUSY verdict the kernel already gave | `packages/cli/src/key.test.ts` ("INVARIANT §17 / §3.4: order A — a LIVE daemon makes rotate refuse non-blocking, naming the holder") + `packages/mcp/src/integration.test.ts` ("INVARIANT §17 / §9.1: `--doctor --offline` REFUSES while a daemon is live, naming the holder") | ✅ pinned |
| §17 / §9.1 — `--doctor --offline` performs zero writes to the PRODUCT DATABASE: it takes maintenance EXCLUSIVE (so it cannot run beside a live daemon or a rotation) and never calls `openStoreFromEnv`, which creates, heals, sets journal mode, migrates, and may bootstrap the key canary — every one a write, and the reason the retired unguarded `--doctor` was a second writer against a database the daemon owns. (It does write its own lock-db holder row, which is diagnostic metadata outside the product db.) Pinned by the only measure that cannot be faked: mtime+size+mode over the db AND its `-wal`/`-shm`/`-journal` sidecars across a run against a REAL migrated database, asserting nothing was modified, created, OR removed (a checkpoint that deletes a sidecar is a mutation too) | `packages/mcp/src/integration.test.ts` ("INVARIANT §17 / §9.1: `--doctor --offline` performs ZERO writes — db and sidecar mtimes unchanged") | ✅ pinned |
| §17 / §3.4 — a missing state directory is ONE shared reading across every maintenance-lock acquirer: nothing can hold a lock inside a directory that does not exist, so `acquireExclusiveIfPresent` answers `no-state-dir` instead of throwing libsql's raw `SQLITE_CANTOPEN`. `key generate` PROCEEDS on it — it is the documented first onboarding step and creates the directory itself moments later — while `key rotate` and `--doctor --offline` fall through to their own typed refusals. The directory is never created by an acquirer: creation is the daemon's prerogative (§3.2), so inspecting can never provision the state it inspects, the same property `probeShared` already guarantees | `packages/cli/src/key.test.ts` ("INVARIANT §17 / §3.4: fresh install — generate works when the state directory does not exist yet" + "INVARIANT §17 / §3.4: fresh install — rotate fails on its OWN terms, never a raw lock error") + `packages/mcp/src/daemon/locks.test.ts` ("acquireExclusiveIfPresent: a missing state directory reads `no-state-dir`, and is not created") | ✅ pinned |
| §17 / §3.4 — the holder row is a LEAD, never a verdict: it is cleared on orderly release so a later refusal cannot name a departed process, and because a SIGKILLed holder inherently leaves its row behind, the surviving row is rendered hedged ("Last acquired by … (may be stale)") rather than asserted. Naming a specific dead pid while a different, role-less process actually holds the lock is worse than naming nobody — it sends the operator after a process that is already gone | `packages/mcp/src/daemon/locks.test.ts` ("INVARIANT §17 / §3.4: the holder row is CLEARED on release — a refusal never names a departed process" + "a surviving holder row is rendered HEDGED — diagnosis, not authority") | ✅ pinned |
| §17 / §9.1 — the default `--doctor` reports the DAEMON's health rather than re-deriving it privately: it reaches the daemon over the socket using the `serve` role's existing `catalog.listing` read (no new capability row — §3.3's authorization surface is unchanged) and prints the daemon's own source count — plus the db path and effective egress setting taken from `handshake.ok` (§9 item 3), the latter being reportable by NO other mode on a live install since `--offline` refuses while a daemon runs — while M8 stdout purity survives the conversion. This is also the more truthful diagnosis: a command that opened its own store would report whether A store opens, not whether THE store the agent will use is healthy | `packages/mcp/src/integration.test.ts` ("INVARIANT §17 / §9.1: the default `--doctor` reports the DAEMON's health, not a private store open" — real spawned daemon, seeded source counted over the socket) | ✅ pinned |

---

**Adversarial review has a stop line.** A cross-model pass over these
invariants has converged (ship) when every finding is either out-of-scope by a
spec §18 decision, or against a layer labeled best-effort defense-in-depth
(e.g. the §9.2 credential-echo scan). Do not extend a denylist-shaped check per
finding — fix the shape (canonicalize, as §9.3 egress now does via
`createPinnedLookup`) or relabel it best-effort. See CLAUDE.md → "Adversarial
review has a stop line" and `~/.claude/rules/adversarial-convergence.md`.
